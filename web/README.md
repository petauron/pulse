# Pulse Web

The Web application will be implemented after the dashboard concept and data contracts are approved. It will be built as static assets and embedded into `pulse-service`; production deployments will not require a Node.js process.
