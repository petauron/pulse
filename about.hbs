<!--
Generated by cargo-about. Do not edit this file directly; update about.toml or
about.hbs and regenerate RUST_THIRD_PARTY_LICENSES.html instead.
-->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Pulse Rust third-party licenses</title>
  <style>
    :root { color-scheme: light dark; }
    body { font-family: sans-serif; line-height: 1.5; margin: 0; }
    main { margin: 0 auto; max-width: 72rem; padding: 2rem; }
    a { color: inherit; }
    pre { border: 1px solid #8886; border-radius: .4rem; overflow-x: auto; padding: 1rem; white-space: pre-wrap; }
  </style>
</head>
<body>
  <main>
    <h1>Pulse Rust third-party licenses</h1>
    <p>This document lists the license texts selected for the Pulse Service and Agent Linux dependencies, including build dependencies. The appendix preserves original license, notice, and copyright files from those packages. Rust standard library notices are distributed separately in RUST_STDLIB_LICENSES.html.</p>

    <h2>License overview</h2>
    <ul>
      {{#each overview}}
      <li><a href="#{{id}}">{{name}}</a> ({{count}})</li>
      {{/each}}
    </ul>

    <h2>License texts</h2>
    {{#each licenses}}
    <section>
      {{#if first_of_kind}}
      <h3 id="{{id}}">{{name}}</h3>
      {{/if}}
      <h4 id="{{id}}-{{@index}}">{{name}}</h4>
      <p>Used by:</p>
      <ul>
        {{#each used_by}}
        <li><a href="{{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}}">{{crate.name}} {{crate.version}}</a></li>
        {{/each}}
      </ul>
      <pre>{{text}}</pre>
    </section>
    {{/each}}
    <!-- ORIGINAL_CRATE_LICENSES -->
  </main>
</body>
</html>
